import React, {useState, useEffect} from "react";
import {Text, View} from "react-native";
import * as db from "../services/DBManager.ts";

export default function ListItem_CommonFoods(props: {foodId: number}) {

	return (
		<View>

		</View>
	);
}
