import React, {useState, useEffect} from "react";
import {Text, View} from "react-native";

export default function ListItem_CommonFoods(props: {foodId: number}): React.JSX.Element {

	return (
		<View>

		</View>
	);
}
